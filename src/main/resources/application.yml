app:
  name: 庆五一程序员闯关大赛
 
server:
  port: 51
spring:
  thymeleaf:
    prefix: classpath:/templates
    encoding: utf-8
  mvc:
    locale: zh_CN
  application:
    name: ${app.name}  session:    timeout: 10m
    